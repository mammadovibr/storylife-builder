# StoryLife Builder

StoryLife Builder v0.1 is a Windows-ready visual editor for interactive text games.

## Commands

```powershell
npm install
npm run dev
npm run build:win
```

Project data is stored in external `.json` files with `version: 1`.
